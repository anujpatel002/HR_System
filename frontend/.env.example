# Backend URL for Next.js rewrites (not exposed to client)
BACKEND_URL=http://localhost:5000

# Note: All API calls should use the /api proxy path
# Do NOT use NEXT_PUBLIC_API_URL as it exposes the backend URL to the client
